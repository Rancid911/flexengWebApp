# Admin Feature

Target home for admin console UI, client hooks, admin-specific server use-cases, validation, and DTOs.

Current migration status: in progress. Existing admin route-local UI still lives under `app/(workspace)/(staff-zone)/admin`.
